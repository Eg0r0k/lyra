# Progress Log

> Update after every completed task: status, commit hash, date, one-line note.
> Statuses: TODO / IN PROGRESS / DONE / DEFERRED / BLOCKED.
> Execution order per amendments.md: T-01 → T-02 → T-06 → T-03 → T-04 → T-05 → rest.

| Task | Title | Status | Commit | Date | Notes |
|------|-------|--------|--------|------|-------|
| T-01 | Stale-load error gating + AbortSignal into strategies | DONE | b9d3f8b | 2026-07-18 | Catch gated by signal identity (swallow stale); `StrategyInitOptions.signal` required + threaded into both strategies; html5 waiters abort-aware; dispose detaches init waiters. +4 tests, mock empty-src error + deferred decode. |
| T-02 | Load-generation guard; autoplay decoupled from load() | DONE | ecf0a2b | 2026-07-18 | breaking → major (load() no longer rejects on autoplay block). play() captures strategy+signal, bails silently when superseded; autoplay branch swallows play() error (single PLAYBACK_NOT_ALLOWED emit, state stays ready). +3 tests, mock deferred-play hook. README note deferred to T-25. |
| T-06 | Playwright matrix + local HLS fixtures | DONE | 27759d0 | 2026-07-18 | e2e/ scaffold: 3 engines + strict-autoplay project, dual-origin fixture server (CORS/no-CORS), tiny ffmpeg fixtures (WAV 32K + fMP4/AAC HLS VOD + live no-ENDLIST). Seed suite 7 pass +1 documented skip. Probed engines: WebKit-Win has NO Web Audio & NO native HLS; Chromium automation doesn't enforce autoplay block. Native-HLS 3-tier scheme + manual notes in e2e/README.md. WAV chosen over MP3/AAC via codec probe. |
| T-03 | Native HLS path for Safari/iOS | DONE | 9d51b21 | 2026-07-18 | New NativeHlsHandler (id `hls-native`): canHandle = isHlsSource && canPlayType('application/vnd.apple.mpegurl'), preferredStrategy html5, prepare {sourceUrl}, getCapabilities {qualityLevels:[],isLive:false}, probe cached per instance. Registered after HLSHandler, before Buffer. Shared `isHlsSource` (hls-source.ts) reused by HLSHandler/SourceManager. Actionable LOAD_NOT_SUPPORTED (mentions hls.js + native). Exported from barrel. +4 tests (3 selection + 1 Player load), test-utils setNativeHlsSupport hook. e2e honest-negative (webkit) still green. |
| T-04 | Web Audio routing policy + CORS rework | DONE | 05a6f0c (+per-load follow-up) | 2026-07-18 | DECISION (user, overrides amendment step1/Q1): explicit `webAudioRouting:'always'\|'never'` (default always), NO usage heuristic. 'always'=route+crossOrigin for cross-origin html5 (EQ/viz/fades work out of box); 'never'=no graph, no crossOrigin, no AudioContext. `corsFallback:false` opt-in retry. Shared isCrossOrigin (utils/url.ts). Follow-up: `LoadOptions` per-load override of `webAudioRouting`+`corsFallback` via `load(src, opts)` → mixed playlists resolve per track (closes F-02 for playlists). +8 vitest (incl. per-load both directions + mixed playlist) + e2e routing.spec — WebKit plays via 'never'. amendments.md Q1 + T-04 rewritten to as-built. DROPPED amendment's "no crossOrigin unless feature used" criterion (unimplementable). |
| T-05 | HLS runtime errors + recovery | DONE | 0234aa3 | 2026-07-18 | HLSHandler: persistent ERROR listener survives readiness resolve; pre-ready fatal fails load (as before), post-ready fatal → recovery. Fatal NETWORK: 3 startLoad() retries w/ 1s/2s/4s backoff, abort-aware via retained signal; FRAG_BUFFERED resets budget. Fatal MEDIA: recoverMediaError, then swapAudioCodec+recover, then give up. Give-up → detachMedia+destroy + surface via new SourceCapabilities.onRuntimeError → Player.handleRuntimeError (emit once + transition error). Dedupe: element error handler skipped when handler exposes onRuntimeError. HlsInstance +startLoad/stopLoad/recoverMediaError/swapAudioCodec; cleanup clears backoff timer. +4 tests (fake timers). e2e green. |
| T-07 | ESLint flat config | DONE | 72b7253 | 2026-07-19 | Flat `eslint.config.js` (typescript-eslint 8.64 + @eslint/js 10 + globals). **Type-aware** for `src/**` (projectService) — deviation from spec's "not type-checked", per user directive to catch `no-floating-promises`/`no-misused-promises`. e2e + `*.config.ts` use non-type-checked recommended (outside src tsconfig). Test override relaxes mock-noise rules (no-explicit-any/no-unsafe-*/unbound-method/only-throw-error/no-unnecessary-type-assertion) but KEEPS bug-signal rules (no-floating-promises) on. Conventions: `_`-prefix ignore for no-unused-vars; require-await off (interface-conformance async); prefer-const `ignoreReadBeforeAssign` (EventEmitter closure). Baseline: 66 errors → 0. **Findings surfaced & FIXED, not suppressed:** no-floating-promises ×2 (both tests, intentional fire-and-forget → explicit `void`; NO new prod floating promise beyond F-17); Logger `any[]`→`unknown[]`; events.ts `(Error as any).captureStackTrace`/`(e as any).message` typed + safe stringify (no-base-to-string); Player `(window as any).webkitAudioContext` typed; no-unnecessary-type-assertion ×4 removed (EventEmitter, AudioGraph). Justified targeted disables ×2: prefer-promise-reject-errors on pass-through re-rejection (CancellationToken.wrap, unlockAudio finish). lint+typecheck+130 tests green. Acceptance: deliberate unused var fails lint (verified). New devDeps: typescript-eslint, @eslint/js, globals. |
| T-08 | pause/togglePlay during buffering (+F-34/F-35 FSM) | DONE | 03cf34e | 2026-07-18 | One commit, 3 changes: F-10 pause() guard `is("playing")`→`isActive`, togglePlay on `isActive` not `isPlaying`; F-34 table +`ready→buffering`+`paused→buffering` (waiting emitter left unguarded); F-35 table +`ready→error`+`paused→error` (handleRuntimeError unchanged). Mirror-hole audit: ready/paused→idle/disposed and stop()/dispose() from paused all ALREADY valid — no gap. Tests separated per finding: state-manager (F-34,F-35 edges), player (F-10 pause+toggle, F-34 waiting ready/paused), hls (F-35 paused+ready runtime error). +8 tests; empirically verified all 8 fail pre-fix, 90 prior unaffected. |
| T-09 | Unified HTML5 readiness waiter | DONE | 430f569 | 2026-07-18 | Extracted one private `waitForReady(signal)` in HTML5Strategy: resolves on earliest loadedmetadata/canplay, rejects on element error / abort / timeout; always removes listeners+timer; sets `_activeInitCleanup`. All 3 branches use it — **sourceUrl path gains the 30s timeout** (F-09). Timeout → `PlayerError(LOAD_NETWORK)` (was bare Error). Const `READINESS_TIMEOUT_MS = 30_000` (not configurable, per Don't-do). readyState>=1 fast-paths kept; waiter lives inside initialize() (before bindStrategyEvents) so F-34 loading→buffering invariant holds. preAttached + no-sourceUrl branches merged (identical logic) → `preAttachedMedia` metadata flag now vestigial (incidental). +2 tests (fake timers): url timeout→LOAD_NETWORK, timeout cleans listeners+timer. e2e green. |
| T-10 | Volume/fade gain split | DONE | 176851c | 2026-07-18 | **breaking → major.** AudioGraph split into fadeGain (`_outputGain`) + new `_volumeGain`; chain input→norm→EQ→analyser→fade→volume→output. `output` getter now returns volumeGain. setVolume/setVolumeImmediate drive volumeGain (no longer cancelFade); fadeTo/cancelFade drive fadeGain only (multiplier 0..1). Player: single `applyVolumeAndMute(immediate?)` replaces getRestingGraphGain + 4 instanceof branches — routed graph ⇒ volume/mute on volumeGain, strategy pinned to unity (fixes iOS html5 volume F-11); un-routed ⇒ element. fadeIn→multiplier 1, fadeOutAndPause/Stop reset fadeGain to 1 (restore-hack removed). Analyser stays pre-volume/fade = UNCHANGED visualizer behavior (see report). Adapted 5 regression tests + 3 new (F-13/F-11/reset). e2e green. |
| T-11 | Harden unlockAudio + auto-resume | DONE | eb10f3f | 2026-07-19 | unlockAudio always settles: resolves on running (statechange listener) OR silent-buffer onended OR immediate running check; rejects PLAYBACK_NOT_ALLOWED after 2s timeout (const UNLOCK_TIMEOUT_MS); `_isAudioUnlocking` reset in `finally` → retry after failure; disconnects placeholder source in every path. Auto-resume in onstatechange gets `.catch` (playerLogger.debug), no retry loop. `_isAudioUnlocked` reset on ctx recreation (getAudioContext closed branch + dispose). Mock: MockAudioContext extends EventTarget, setState dispatches statechange event, resume() honors static resumeError/resumeKeepsState hooks, +createBuffer. +4 tests (running resolves, stuck-suspended rejects+retries, auto-resume rejection contained via process.on guard, latch resets on recreation); all 4 fail pre-fix. Deviations: real setTimeout macrotask flush + executor-form Promise in the containment test (rules ts-no-test-timers/ts-promise-with-resolvers) — Node unhandledRejection needs a real macrotask boundary + repo lib is ES2020 (no Promise.withResolvers). |
| T-12 | Clear loudness metadata on load | DONE | 3c0dc9d | 2026-07-19 | `_loudnessMetadata` cleared in load() right after cleanup(), before recomputeNormalization, unless `loudnessNormalization.retainMetadataAcrossLoads` (new option, default false). recompute's existing null-branch resets gain to 0 dB + emits `normalizationchange{enabled:false}`. Reset is instantaneous-in-effect (NOT touching gain math): graph is silent between cleanup (old source disposed) and new-track start (new source not started until play()), and cleanup already settled the norm node to unity during the async prepare/decode gap — so the shared smoothed `resetNormalization` ramps unity→unity (no smear); left smoothed because mid-playback callers (setNormalizationEnabled(false)/clearLoudnessMetadata) want it. retain=true ⇒ gain is RECOMPUTED from retained metadata+options (not raw node carry-over). +2 tests (F-18 reset — fails pre-fix; retain opt-out guard). Doc (per-track workflow) deferred to T-25 (open-threads.md). Non-spec deviations: none. |
| T-13 | Explicit mode wins over handler preference | DONE | a1bf313 | 2026-07-19 | New optional `ISourceHandler.requiredStrategy?(): 'html5'\|'webaudio'\|undefined` (HARD override) documented alongside `preferredStrategy` (soft auto-only hint). HLSHandler + NativeHlsHandler return 'html5'. Player.load resolution: explicit mode used as-is, overridden ONLY by `handler.requiredStrategy?.()` (optional-call safe on custom handlers lacking it, F-14); auto still goes through recommendStrategy (unchanged, consults preferredStrategy). Replaced the old preferredStrategy hard-override block. Non-breaking (optional interface addition). +3 tests: F-14 (explicit html5 + ArrayBuffer → html5 via blob URL — fails pre-fix), HLS overrides explicit webaudio + warn (guard), custom handler w/o requiredStrategy registered via registerHandler → explicit html5 honored + no crash, preferredStrategy still hints in auto (fails pre-fix). typecheck+114 tests green. Deviations: none. Test uses named cast to reach private `_sourceManager.registerHandler` (Player exposes no public registration — incidental finding). |
| T-14 | Handler lifecycle: reset vs dispose | DONE | 8f1cb3e | 2026-07-19 | Split per-load teardown from terminal teardown. `ISourceHandler.reset?(): void` added (optional-with-fallback, non-breaking) + lifecycle doc (constructed once → prepare per load → reset between loads → dispose once). HLSHandler: old private `cleanup()` → public `reset()` (destroy hls session + timers, keep `_Hls`); `dispose()` = reset + null `_Hls` (terminal); all internal callers (prepare/abort/error/surfaceFatal) → `reset()`. Player.cleanup() now `_currentHandler?.reset?.()` (was `dispose()`, the F-15 landmine); SourceManager.dispose() stays the only dispose() caller. **F-37 closed**: added public `Player.registerHandler(handler)` delegating to `_sourceManager` — external handlers were unreachable (private SourceManager, no injection), yet AGENTS.md + audit treated registerHandler as public. Updated T-13 test to use the public method (dropped private cast). +4 tests: HLS fresh-session reuse (hls.test), reset-between-loads + no per-load dispose (F-15), dispose-once-terminal, registerHandler reachability (F-37) — all fail pre-fix (verified by reverting cleanup→dispose). typecheck+118 green. Docs (registerHandler + lifecycle) deferred to T-25. Deviations: built-in Url/Blob/Buffer/NativeHls handlers omit reset() (no per-load state) rather than adding noop stubs — optional-with-fallback makes stubs redundant (spec said "reset() noop"; equivalent, less noise). |
| T-15 | Live HLS: isLive, Infinity, seekable | DONE | 3ed167d | 2026-07-19 | `TimeSeconds` now permits Infinity (rejects only NaN/negatives) — fixes the brand launder that zeroed live durations (F-08). HLSHandler derives `_isLive` from `LEVEL_LOADED.details.live`, retains the media element, and exposes `isLive` + `getSeekableRange()` (new optional `SourceCapabilities.getSeekableRange`); reset clears both. NativeHlsHandler: `isLive = element.duration === Infinity`, `getSeekableRange` from element, captures element in prepare (+reset). `Player.isLive` getter (from active capabilities). `Player.seek`: live → clamp to seekable range, no-op+debug when empty; VOD → unchanged [0,duration] clamp. Progress gated on `isLive` (0 for live) — see deviation. +6 unit tests (branded Infinity/neg/NaN; HLS live isLive+Infinity-duration mock; live seek clamp+empty no-op; VOD regression) — pre-fix failures confirmed for the F-08 catchers. e2e `hls.spec.ts` live test reaches REAL segment playback on chromium (isLive, finite window duration, progress 0, clamped seek) — 3/3 chromium, full browser suite 11 passed/1 skipped. typecheck+125 unit green. **Deviations**: (1) progress gated on `isLive` not `duration===Infinity` (spec step 3 wording) — hls.js live reports a FINITE sliding-window duration, so Infinity-gating would leak non-zero progress; isLive-gating is correct for both hls.js (finite) and native HLS (Infinity). (2) e2e asserts finite window duration for hls.js live (Infinity is native-HLS-only, covered by the jsdom unit test) — the Infinity contract holds for native HLS, not hls.js. |
| T-16 | hlsConfig passthrough + setQuality polish | DONE | 72cd963 | 2026-07-19 | **F-19**: `hlsConfig` widened to `Partial<HLSConfig> & Record<string, unknown>` (PlayerOptions/Resolved/SourceManagerOptions/HLSHandler); prepare now passes the full merged config `{ ...this._config }` to `new Hls()` — arbitrary keys + `startFragPrefetch` (was dead) forwarded. **F-20 + LEVEL_SWITCHED relay (user directive)**: investigation confirmed NO `LEVEL_SWITCHED` subscription existed → after `setQuality(-1)` the consumer never learned the auto level (a flaw, not a contract choice). Added `SourceCapabilities.onQualityChange`; HLSHandler subscribes to `LEVEL_SWITCHED`, relays the REAL `_qualityLevels[data.level]`; Player.load registers it → emits `qualitychange`. `setQuality` now validates (`-1 \|\| levels[level]`), returns `boolean`, and does NOT emit synchronously — the engine-selected level arrives async via the relay (works for explicit AND auto; single source of truth, no synthetic/duplicate emit). Mocks: hls.test MockHls +LEVEL_SWITCHED (currentLevel setter fires it) +config capture. Tests: adapted setQuality test (boolean + async relay + auto emits index 0 + invalid→false), +F-19 passthrough test; +e2e `hls.spec.ts` proving real hls.js LEVEL_SWITCHED→qualitychange (4/4 chromium). lint+typecheck+134 unit green. Docs→T-25. Deviations: removed synchronous explicit-level emit (spec step 3 wording) in favor of the async LEVEL_SWITCHED relay — per user guidance (real level, no duplicates, auto works). |
| T-17 | Smooth EQ param updates | DONE | 58f56fb | 2026-07-19 | `setEQBand` + `resetEQ` switched from `setValueAtTime` (stepwise → zipper noise) to `setTargetAtTime(gain, now, 0.015)`, matching `setEQEnabled` (F-21). Doc comments added. `getEQBand` unchanged (target from `_bands`). Mock: `MockAudioContext.createdBiquadFilters` tracking added. +1 test (band update calls setTargetAtTime, NOT setValueAtTime — fails on regression to stepped). lint+typecheck+133 green. Deviations: none. |
| T-18 | Seek/timeupdate parity | TODO | | | |
| T-19 | Injectable AudioContext | TODO | | | |
| T-20 | Element/source-node reuse | RISK ACCEPTED | — | 2026-07-19 | Not taken. Return condition ("after T-03/T-05 settle") is formally met, but the decision stands on design grounds, not immature deps: one shared `HTMLAudioElement` captured in turn by HTML5 strategy + hls.js + native HLS is a residual-state bug magnet (leftover src/listeners/preservesPitch/rate/crossOrigin; MediaElementSourceNode pinned once-per-element). F-24 = low-medium leak with NO measured profile. **Review condition:** revisit only if a reproducible complaint about growing memory in long playlists appears (measure first). Consciously opted out — see fable-audit.md T-20. |
| T-21 | sideEffects + size tracking | TODO | | | |
| T-22a | PlaybackRate clamp, logger, transition logging | DONE | eef9460 | 2026-07-19 | Four findings (F-26, F-31, F-33 + F-36 folded from T-09 incidental). **F-26**: `PlaybackRate` now clamps every FINITE value (incl. 0/negatives) into [0.0625,16]; only non-finite → 1 (was `value<=0 → 1`). Doc comment updated. **F-33**: HLSHandler `console.debug` → `playerLogger.debug`; grep-verified no raw `console.*` outside Logger.ts. **F-31**: new `Player.transitionTo(to, op)` helper captures `StateManager.transition` result at the 4 load-critical sites (loading/ready/error=load, playing=play), `playerLogger.debug` with op name when rejected; StateManager warn kept. **F-36**: removed vestigial `metadata:{preAttachedMedia:true}` from HLSHandler.prepare — confirmed INTERNAL (write-only dynamic key, read nowhere; HTML5 fast-path uses readyState; untyped `Record` bag so no type-surface change) → non-breaking, correctly T-22a not T-22b. Tests: adapted player.test setPlaybackRate(0)→0.0625 +NaN→1 (old 0→1 behavior updated, guard preserved); +2 branded PlaybackRate cases (fail pre-fix). lint+typecheck+132 green. Deviations: none. |
| T-22b | Remove ReadableStream from AudioSource.data | TODO | | | breaking → major, with T-26 |
| T-23 | Explicit preservesPitch (HTML5) | DONE | 7cdcf63 | 2026-07-19 | `PlayerOptions.preservesPitch?` (default true) + `Player.setPreservesPitch(v)` + `get preservesPitch()` (intent) + `get canPreservePitch()` (reflects active strategy). `StrategyInitOptions.preservesPitch` threaded; both strategies implement `setPreservesPitch`+`canPreservePitch`. HTML5Strategy: applies on initialize AND on toggle **to the live element immediately** (the catch — verified by a guard test that fails if deferred), standard `preservesPitch` with `webkit`/`moz` vendor fallback via dynamic-key feature detection (avoids DOM-lib `never`-narrowing); canPreservePitch = any pitch prop present. WebAudioStrategy: stores intent, `canPreservePitch=false`, warns once (playerLogger) when preservesPitch && rate≠1 (no stretcher until T-24). Player.setPreservesPitch applies to `_currentStrategy` immediately + carries into loads. Mocks: MockAudioElement vendor-configurable pitch prop (`setMockPitchVendor` standard/webkit/moz/none). +5 unit tests (html5 init default true; **live toggle** applies to element now; webkit vendor fallback; webaudio canPreservePitch false; webaudio warn-once). typecheck+130 green. Real audible pitch = manual (e2e/README checklist). Docs → T-25. Deviations: vendor access via `as unknown as Record<string,unknown>` + dynamic key (DOM lib types `preservesPitch` as always-present, narrowing vendor branches to `never`). |
| T-24 | Time-stretch plugin API (WebAudio) | TODO | | | interface + DI only |
| T-25 | README overhaul | TODO | | | after deps. **Fades section** must document (see open-threads.md): `fadeOut()` without pause/stop leaves the fade multiplier at 0 while playback continues (raising volume won't restore sound; recovery is `fadeIn()`); new `player.fadeMultiplier` getter (0..1) beside `isFading`; fade and volume are independent multipliers (T-10). |
| T-26 | Release hygiene: CHANGELOG, major, Migration | TODO | | | NEW, runs last. **Breaking changes to record for the major:** (a) T-02 — `load()` no longer rejects when only autoplay is blocked (resolves ready + one PLAYBACK_NOT_ALLOWED error event). (b) T-10 — `AudioGraph.fadeTo` is now a fade **multiplier** on top of volume (was the absolute output gain); `AudioGraph.setVolume`/`setVolumeImmediate` drive a separate volume gain and no longer cancel an active fade; `AudioGraph.output` now returns the volume gain (end of chain); routed html5 applies volume via the graph and pins the element to unity (element `.volume` no longer reflects `player.volume` when routed). (c) T-22b — `ReadableStream<Uint8Array>` removed from `AudioSource.data`. Plus semi-breaking T-14 (`handler.reset?()`). |
